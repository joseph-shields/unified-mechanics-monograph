# Open ledger additions

**What this limb contributes to the corpus, and what it owes.**
29 July 2026

---

## Four papers to be issued into `02 TECHNICAL PAPERS`

These are the results that stand on their own and close or sharpen existing corpus items.
They are listed here rather than in the limb because the corpus's grading should apply to
them.

### Paper 27 · The cell and the two lengths

**PROVED.** The three weights are a perfect square, `u² + 2ur + r² = (u+r)² = 1`, and they
are the same split as the carrier's antisymmetric square: `u²` on the 10, `2ur` on the 15,
`r²` on the 3. Each cell carries the whole block structure.

A least-action cell fixed by `ε τ = ℏ`, `a = c τ` and `ρ_Λ = ε₀/a₀³` has size

```
a₀ = (ℏc/ρ_Λ)^(1/4) = 87.78 μm        whole cell
                    = 48.80 μm        matter fraction W_M
```

and satisfies `l_P · L_Λ = a₀²` exactly, so

> **Newton's constant and the cosmological constant problem are the same missing number.**

One ratio, 5.431×10³⁰, appearing once as `a₀/l_P` and once as `L_Λ/a₀`. The 122 orders of
the cosmological constant problem are that ratio to the fourth power, exactly.

*Note the identity is dimensional and holds in any theory. What the framework adds is the
claim that the middle length is the size of an object; granting that, the identity does the
work.*

### Paper 28 · The compression boundary

**PROVED.** The map from the four field scalars to the three geometric potentials has rank
3 and nullity 1, with kernel `α_L → α_L + δ`, `ν → ν − 3δ`. So

> **General relativity fixes both metric potentials and can never separate the light mode
> from the density mode, at any precision whatsoever.**

Reached twice independently: once as a kernel computation, once from the slip parameter
coming out identically 1 for every response ratio.

Carries the exact isotropic Einstein equations in the field variables, Schwarzschild
residuals of exactly zero, and a constant-density star reconstructed end to end.

### Paper 29 · Where the mass operator lives

**PROVED, and it closes a line the corpus has been pushing since Paper 21.**

No element of `so(3) + so(4,1)` has three distinct nonzero real eigenvalues on the 15.
Two hundred random elements, maximum reached: two. The reason is structural, since family
weights are purely imaginary and only zero-family-weight states carry a real part.

> **Attacks on the mass problem were looking for three masses in a place that structurally
> cannot hold them.**

The operator is in the boundary block. Because `[15,15] ⊂ 3 + 10`, `ad(X)` has no
15-to-15 component and the operator is `ad(X)²`, which is also right physically since an
action carries mass squared. On the three (family, time) directions it gives exactly

```
m² = v₁² + v₂² + v₃²
```

a Pythagorean relation with one eigenvalue. **The families are components of the operator,
not eigenvalues of it**, which is why no spectrum ever showed three. The remaining twelve
components carry the splitting.

### Paper 30 · The composition bound

**PROVED.** Joining two strings through the partition shifts the phase by
`arg((W_L + W_M) + W_B e^(iδ))`, bounded by

```
W_B/(W_L + W_M) = 2ur/(u² + r²) = √5/3 = 1/Λ        exactly, verified symbolically
|shift| ≤ arcsin(1/Λ) = 48.189685°
```

where `Λ = 3/√5` was derived years earlier from MacDowell-Mansouri. **A second, unrelated
route to the same constant.**

A wound string closes when `n · shift = 360 m`, so the tightest closed object needs
`360/arcsin(1/Λ) = 7.470` traversals, hence **eight, the carrier dimension**. Seven is
excluded at 51.43°, eight admitted at 45°, each by about three degrees.

Also: self-composition has exactly two fixed points, at relative phase 0 and π, both
restoring. Two classes, symmetric and antisymmetric.

---

## One live bet for the open ledger

**Newton's constant from the vacuum record.**

```
q = r²⁴¹ (1 − r³)²        →  G = 6.6644×10⁻¹¹     CODATA 6.6743×10⁻¹¹     −0.149%
```

The exponent is not fitted: 240 is the root count the corpus derives, plus one for the
external readout, against an observed effective exponent of 241.0497. The fitted object is
the residual prefactor 0.9433, matched by `(1 − r³)² = 0.9419`.

**Falsifier, and it is a real one.** All the uncertainty is in Λ, at 1.5 per cent, while G
is known to 0.002 per cent. Any improvement in Λ sharpens this into a test, and a
hundredfold improvement would test it at 0.015 per cent against a G known 700 times better.
**Cosmology settles this, not algebra.**

---

## What this limb owes

1. **The microscopic matter-to-cell law.** The source sector is exact given the two metric
   potentials. What is missing is how matter sets the cells inside itself. This is the one
   step between representing general relativity and deriving it.
2. **The twelve splitting components** of the mass operator, fixed by stationarity under
   the same action that gave `B₁₅ = −R₁₅`.
3. **Names for the closure objects.** The colour field counts them and orders them by
   depth. Attaching a mass or a charge to a depth is the same obligation as (2), reached
   from the other side. **Two lines, one obligation.**
4. **Sub-millimetre gravity.** Compare 48.8 μm against the published apparatus response of
   the torsion-balance experiments, since existing bounds are set for Yukawa deviations and
   a granularity signature is a different observable.
5. **Beyond spherical.** The reconstruction is exact in the spherical scalar sector. Vector
   and tensor modes need the phase congruence or a fuller tetrad construction.

---

## Withdrawn, and recorded

A folder like this is worth most when it says what has already been tried.

| | |
|---|---|
| the action lock `T·P = 1` | asserted, not derived. `ε τ = ℏ` is constant by definition; the cell area `τ·a = cτ²` is not the action. |
| "Schwarzschild derived with no field equation" | the profile was supplied to the integrator. What stands is the exact *representation*. |
| the two-body force calculation | started from the Newtonian potential and recovered Newton. A change of variables. |
| the QCD string tension | `σ = ρa² = E²/(ℏc)` identically, so supplying a scale and recovering the tension is a unit conversion. |
| crowding energy as dark matter | short by 10¹¹ in galaxies, and structurally: proportional to `ρ_Λ`, so it cannot exceed the vacuum energy times a small potential. |
| the charged-lepton mass formulas | the muon candidate misses by 0.83 per cent against a quantity measured to two parts in 10⁸. |
| the electroweak VEV prefactor | three chosen factors for 0.1 per cent. The geometric-mean bridge `√(E_P E_0)` survives; the prefactor does not. |
